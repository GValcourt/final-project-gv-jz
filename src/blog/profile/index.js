
//display profile based on user id, options for logged in vs anonymous access

function ProfileComponent(){
    return (
        <div>
            ProfileComponent
      </div>
      );
}

export default ProfileComponent